version: 2

build:
  os: ubuntu-24.04
  tools:
    rust: latest
  commands:
    - curl --fail --location https://mise.run | sh
    - MISE_SAFE=1 ~/.local/bin/mise install --locked jq just node zola
    - MISE_SAFE=1 ~/.local/bin/mise exec -- just site-readthedocs
